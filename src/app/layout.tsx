import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Squeeze, Sip, Repeat: SUCKA PUNCH and the New Wave of Alcoholic Hydration",
  description: "Discover how SUCKA PUNCH is redefining the way we hydrate with alcohol. This unique drink combines the refreshing benefits of water with the fun and social aspects of alcohol, making it a perfect choice for those who want to enjoy a drink while staying hydrated.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
